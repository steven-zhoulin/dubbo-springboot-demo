/* 子模块项目名，jar名 */
archivesBaseName = 'order-svc'

jar.enabled = true
/* 需要org.springframework.boot插件 不需要打包成可执行的jar*/
bootJar.enabled = true

dependencies {
    compile project(':order-api')
    compile project(':upc-api')

    compile fileTree(dir: '../libs', include: ['*.jar'])

}